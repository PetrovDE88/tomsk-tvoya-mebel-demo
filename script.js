document.querySelectorAll(".options").forEach((group)=>{group.querySelectorAll("button").forEach((button)=>button.addEventListener("click",()=>{group.querySelectorAll("button").forEach((item)=>item.classList.remove("active"));button.classList.add("active");const chosen=[...document.querySelectorAll(".options button.active")].map((item)=>item.textContent.trim());document.querySelector("#selection").textContent=chosen.join(" · ")}))});
document.querySelector("form")?.addEventListener("submit",(event)=>{event.preventDefault();event.currentTarget.querySelector(".success").hidden=false});
